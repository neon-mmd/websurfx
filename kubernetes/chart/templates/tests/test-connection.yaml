apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "websurfx.fullname" . }}-test-connection"
  labels:
    {{- include "websurfx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "websurfx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
