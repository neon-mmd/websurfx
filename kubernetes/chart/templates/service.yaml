apiVersion: v1
kind: Service
metadata:
  name: {{ include "websurfx.fullname" . }}
  labels:
    {{- include "websurfx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "websurfx.selectorLabels" . | nindent 4 }}
